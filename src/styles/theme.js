const fontSize = {
  xs: '0.5rem',
  sm: '0.75rem',
  md: '1rem',
  lg: '1.25rem',
  xl: '1.5rem',
};

const fontWeight = {
  xs: 100,
  sm: 300,
  md: 500,
  lg: 700,
  xl: 900,
};

const spacing = {
  xs: '0.5rem',
  sm: '0.75rem',
  md: '1rem',
  lg: '1.25rem',
  xl: '1.5rem',
};

const colors = {
  primary: '#2b90d9',
  white: '#ffffff',
  black: '#282c37',
  gray: '#d9e1e8',
};

const theme = {
  fontSize,
  fontWeight,
  spacing,
  colors,
};

export default theme;
